# V1_SpatialModulation
 Scripts for analyzing spatial modulation responses in V1
## Install
Install the conda environment by running conda env create -f environment.yml. Then, run conda activate JSY_SpatialMod followed by pip install -e . to install the twopTools module in this repository as an editable package in the conda environment (must run the pip install -e . command from the path of this repository).

